# Encounter Difficulty Calculator

A standalone D&D 5.5 calculator for party thresholds and one multi-monster
encounter. It validates monster inputs, totals XP, ranks difficulty live, and
supports safe optional statblock links. State resets on refresh.

Run it with the repository-level `npm run start`; production output is built
under `dist/encounter-difficulty-calculator/`.
