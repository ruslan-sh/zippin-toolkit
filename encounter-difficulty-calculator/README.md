# Encounter Difficulty Calculator

A standalone page for the planned D&D 5.5 party-threshold and encounter-XP
calculator. Calculation controls and behavior will be added in later slices.

Run it with the repository-level `npm run start`; production output is built
under `dist/encounter-difficulty-calculator/`.
