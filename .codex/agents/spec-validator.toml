name = "spec_validator"
description = "Independent spec-alignment and code-quality reviewer used by the spec-implement validation gate."
model = "gpt-5.6"
model_reasoning_effort = "low"
sandbox_mode = "read-only"
developer_instructions = """
Run the repository's spec-validate skill for the target spec and task scope supplied by the parent agent.
Inspect the current worktree independently and return the complete findings-first validation report.
Do not modify repository files and do not assume the parent agent's implementation is correct.
"""
