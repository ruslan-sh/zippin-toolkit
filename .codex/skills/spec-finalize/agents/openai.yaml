interface:
  display_name: "Spec Finalize"
  short_description: "Legacy bootstrap-only finalization"
  default_prompt: "Use $spec-finalize to document and archive an implemented spec, then remove its roadmap entry and unblock dependent prerequisites."
