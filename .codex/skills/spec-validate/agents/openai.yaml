interface:
  display_name: "Spec Validate"
  short_description: "Validate spec alignment, then review code quality."
  default_prompt: "Use $spec-validate to validate implementation against the target spec, then perform a focused general code review when no major spec gaps remain."
